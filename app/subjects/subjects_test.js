'use strict';

describe('myApp.subjects module', function() {

  beforeEach(module('myApp.subjects'));

  describe('subjects controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var subjectsCtrl = $controller('SubjectsCtrl');
      expect(subjectsCtrl).toBeDefined();
    }));

  });
});