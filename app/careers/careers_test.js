'use strict';

describe('myApp.careers module', function() {

  beforeEach(module('myApp.careers'));

  describe('careers controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var careersCtrl = $controller('CareersCtrl');
      expect(careersCtrl).toBeDefined();
    }));

  });
});